smsid:
  '#type': 'textfield'
  '#title': 'SMS ID'
  '#required': 'true'

actions:
  '#type': 'actions'
  submit:
    '#type': 'submit'
    '#value': 'Send'
    '#button_type': 'primary'

'#theme': 'system_config_form'
